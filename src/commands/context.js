"use strict";
// Phase 2 — Context Layer

function run(opts) {
  if (opts.preview) {
    console.log("[Phase 2] context --preview — not implemented yet.");
    return;
  }
  if (opts.edit) {
    console.log("[Phase 2] context --edit — not implemented yet.");
    return;
  }
  console.log("[Phase 2] context — not implemented yet.");
  console.log("Coming in Phase 2: fetch project_context from Supabase and display it.");
}

module.exports = { run };
