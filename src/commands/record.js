"use strict";
// Phase 3 — Shadow Branch

function run(reason) {
  console.log("[Phase 3] record — not implemented yet.");
  if (reason) console.log(`Reason given: "${reason}"`);
  console.log("Coming in Phase 3: write a decision record to prepcli/shadow/v1");
  console.log("linked to the current commit.");
}

module.exports = { run };
